//process.stdout.write('prompt > ');

module.exports = {
    pwdFunc() {
        //const cmd = data.toString().trim();
        console.log(process.cwd());
        //process.stdout.write('\nprompt > ');
    }
}

